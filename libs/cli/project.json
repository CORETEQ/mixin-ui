{
  "name": "cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/{projectName}",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p ./{projectRoot}/tsconfig.json && cp libs/cli/*.md dist/libs/cli/"
      }
    }
  }
}

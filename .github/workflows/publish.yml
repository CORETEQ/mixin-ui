name: 📦 Publish

on:
  workflow_dispatch:
  push:
    tags:
      - v*.*.*

jobs:
  version:
    name: Publish new version
    timeout-minutes: 5
    runs-on: self-hosted
    permissions:
      contents: write
      packages: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install
        uses: ./.github/workflows/composite

      - name: Build packages
        run: pnpm build

      - name: Set up npm auth for Nexus (basic auth)
        run: |
          echo "@mixin-ui:registry=https://repo.coreteq.cloud/repository/libs-release-npm/" >> ~/.npmrc
          echo "//repo.coreteq.cloud/repository/libs-release-npm/:_auth=$(echo -n '${{ secrets.NPM_USERNAME }}:${{ secrets.NPM_USERNAME }}' | base64)" >> ~/.npmrc
          echo "//repo.coreteq.cloud/repository/libs-release-npm/:email=${{ secrets.NPM_EMAIL }}" >> ~/.npmrc
          echo "//repo.coreteq.cloud/repository/libs-release-npm/:always-auth=true" >> ~/.npmrc

      - name: Publish packages
        run: pnpm ci:publish
        shell: bash

      - name: Send email notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.office365.com
          server_port: 587
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "🚀 Mixin UI ${{ github.ref_name }} has been released"
          to: fe-devs@coreteq.sk
          from: <${{ secrets.SMTP_USERNAME }}>
          secure: false
          body: |
            New release available!

            🔖 Version: ${{ github.ref_name }}
            🔗 Commit: https://github.com/${{ github.repository }}/commit/${{ github.sha }}

            📘 Release Notes:
            https://github.com/CORETEQ/mixin-ui/releases/tag/${{ github.ref_name }}

            🧩 Documentation:
            https://mixin-ui.dev

            — Mixin UI Bot
